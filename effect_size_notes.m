%%%%%% . effect sizes

% effect size for a one-sample ttest or paired (dependent) sample t-test:

% cohen's d: 

% [mean(X1) - mean(X2)] / std(X1-X2)

% or: 

% mean(X1-X2) / std(X1-X2)

%%%%%%%%

T=readtable('controls_hunger.csv');